/**
 * 资源加载状态
 */
export enum ResLoadStatus {
    unkonw = 0,
    loading = 1,
    loadSuccess = 2,
}

export enum ResType {
    common = 0,
    bg = 1
}
export enum GameStatus {
    unStart = 0,
    start = 1,
    success = 2,
    fail = 3
}