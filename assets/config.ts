let resConfig = {
    altasArr: ['main'],
    jsonArr: [],
    prefabArr: ['bul'],
}
let uiName = {
    messageBox: 'messagebox_ui',
    winPage: 'winPage',
    failPage: 'failPage'
}
let animaParams = {
    'player_run': {
        sample: 3,
        speed: 3,
        wrapMode: cc.WrapMode.Loop
    }
}
export default { animaParams, uiName, resConfig }